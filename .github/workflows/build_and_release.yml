name: Build and Publish EXE

on:
  push:
    branches:
      - main
    paths:
      - 'app.py'
      - 'lang_funcs.py'
      - 'app.spec'
      - 'templates/**'
      - 'static/**'
      - '.github/**'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Run PyInstaller to create EXE
        run: |
          pyinstaller app.spec
        shell: cmd

      - name: Create release artifact
        uses: actions/upload-artifact@v2
        with:
          name: jarvis
          path: dist/Jarvis.exe  # Path for the EXE created by PyInstaller

  release:
    needs: build
    runs-on: windows-latest

    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v2
        with:
          name: jarvis

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v1.0.${{ github.run_number }}
          release_name: "Jarvis Server EXE Build #${{ github.run_number }}"
          draft: false
          prerelease: false

      - name: Upload EXE to Release
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: dist/Jarvis.exe  # Ensure you reference the correct location
          asset_name: Jarvis.exe
          asset_content_type: application/octet-stream
